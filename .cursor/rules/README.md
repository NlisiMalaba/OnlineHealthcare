# Cursor rules for Online Healthcare

This folder is **tracked in git** (see root `.gitignore`) so the whole team shares the same AI and editor guidance.

## Files

- `healthcare-*.mdc` — architecture, security, testing, naming, API patterns, Python AI.
- **`planning/mvp.md`** — MVP scope and Zimbabwe pilot defaults (task prioritisation, deferred features).

Edit the `.mdc` files when standards change; keep `mvp.md` in sync when MVP boundaries change.
