apiVersion: v1
kind: Namespace
metadata:
  name: data
  labels:
    app.kubernetes.io/part-of: health-platform
    # Data plane workloads; Istio optional — omit sidecar if operators prefer
    istio-injection: enabled
